apiVersion: v1
kind: Service
metadata:
  name: rollout-demo
  namespace: rollout-learning

spec:
  type: NodePort

  selector:
    app: rollout-demo

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
