apiVersion: v1
kind: Service
metadata:
  name: whoami
  namespace: rollout-learning
spec:
  selector:
    app: whoami
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: whoami-preview
  namespace: rollout-learning
spec:
  selector:
    app: whoami
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
