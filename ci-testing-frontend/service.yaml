apiVersion: v1
  kind: Service
  metadata:
    name: ci-testing-frontend
    namespace: default
  spec:
    selector:
      app: ci-testing-frontend
    ports:
      - port: 8080
        targetPort: 8080
    type: ClusterIP
